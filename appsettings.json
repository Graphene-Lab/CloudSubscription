{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ApiEndpoint": "http://localhost:5050/api", //Tell the cloud server to send the API commands to (it is recommended to use https if the machine is outside the Intranet)
  "ApiPrivateKey": null, //The RSA private key used to sign JSON requests to the cloud server. Leave null to not sign requests. If this parameter is set on the cloud, the public key for request validation must be set.
  "PayPalBusinessEmail": "sb-zfhv840062342@business.example.com"
  //"Kestrel": {
  //  "Endpoints": {
  //    "Https": {
  //      "Url": "https://cloud.tc0.it:443",
  //      "Certificate": {
  //        "Path": "/etc/letsencrypt/live/cloud.tc0.it/fullchain.pem",
  //        "KeyPath": "/etc/letsencrypt/live/cloud.tc0.it/privkey.pem"
  //      }
  //    }
  //  }
  //}
}
